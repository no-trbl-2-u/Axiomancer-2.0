# Server Configuration
PORT=3001
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=24h

# Database Configuration (for STG/PROD with PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=axiomancer
DB_USER=postgres
DB_PASSWORD=password